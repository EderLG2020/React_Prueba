export default function Task() {
  const cardStyle = {
    background: "#202020",
    color: "#fff",
  };

  return (
    <div style={cardStyle}>
      <h1>Mi primer a tarea</h1>
      <p>Tarea_ continua</p>
    </div>
  );
}
